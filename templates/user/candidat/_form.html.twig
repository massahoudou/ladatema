{{ form_start(form) }}
<div>
    <div class="col-md"></div>
</div>
<div>
    <button class="btn btn-primary" type="submit">
        Enregistrer <span> <i class="fa fa-save"></i></span>
    </button>
</div>
{{ form_end(form) }}
