{{ form_start(form) }}

<div class="text-danger ">{{ form_row(form.afficher) }}</div>

<div class=""> {{ form_row(form.titre) }} </div>
<div class="">{{ form_row(form.type) }}</div>
<div class=""> {{ form_row(form.description) }} </div>
<button  type="submit"class="btn btn-warning rounded-0">
    Enregistrer
    <span class="fa fa-save"></span>
</button>
{{ form_end(form) }}